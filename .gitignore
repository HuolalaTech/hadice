build/bin
node_modules
frontend/dist
dist/
.task/
bin/
!assets/windows/*/bin/
!assets/windows/*/bin/**
.idea/
/hadice-cli
.codegraph/
.cache
.gocache

# macOS junk
.DS_Store

# Local brainstorm / agent tooling
.superpowers/
.cursor/skills/
design-system/

# Private CI / local secrets (copy from .env.ci.example)
.env.ci
frontend/.env
frontend/.env.local
frontend/.env.*.local

# Local secrets / signing materials (do not ship)
build/darwin/sign/
*.p8
*.p12
*.provisionprofile
*.mobileprovision

# Agent compile artifacts (generated by `task build:agent` / agent/build.sh)
agent/build/
agent/include/helper_dex.h
